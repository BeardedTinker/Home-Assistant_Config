#####
# Light history
#####

platform: history_stats
name: Kitchen Lights today
entity_id: light.kitchen_lights
state: 'on'
type: time
start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
end: '{{ now() }}'