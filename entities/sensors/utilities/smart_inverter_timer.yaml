#####
# Track time Smarrt Inverter was in On state
#
#####

platform: history_stats
name: Smart Inverter Daily
#friendly_name:
entity_id: binary_sensor.smart_inverter_on
state: "on"
type: time
start: "{{ now() - timedelta(hours=24) }}"
end: "{{ now() }}"