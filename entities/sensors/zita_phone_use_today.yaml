#####
# Track how much time Zita was on phone today
#
#####

platform: history_stats
name: Zita's Phone use today
entity_id: binary_sensor.ele_l29_interactive
state: 'on'
type: time
start: '{{ now().replace(hour=0, minute=0, second=0) }}'
end: '{{ now() }}'
